apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: statique-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ayarinesrine/pfe-deployment.git
    targetRevision: main
    path: helm-charts/frontend/statique
    helm:
      valueFiles:
        - prod/values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
